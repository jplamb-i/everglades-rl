from setuptools import setup, find_packages

setup(name='gym_everglades',
      version='0.1.0',
      author='Cubic Defense Applications Inc',
      packages=find_packages(),
      include_package_data=True,
      install_requires=['gym']
)
